<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
  <div class="container">
    <%= link_to 'ChatApp', root_path, class: 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item"><%= link_to 'Users', users_path, class: 'nav-link' %></li>

        <%# Only display the current user's name, friend requests and log out links if a user is logged in %>
          <% if user_signed_in?%>
            <li class="nav-item">Welcome <%=current_user.email %></li>
            <li class="nav-item"><%= link_to 'Chatrooms', '/chatrooms', class: 'nav-link' %></li>
            <li class="nav-item"> <%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
          <% else %>
        <%# Only display the sign up and log in links if no user is logged in %>
            <li class="nav-item"> <%= link_to "Sign Up", new_user_registration_path %>  </li>
            <li class="nav-item"><%= link_to "Log In", new_user_session_path %> </li>
          <%end%>
      </ul>
    </div>
  </div>
</nav>